# Sprint S02 - Tipos base + errores + SadConfig completo

## Objetivo (1 frase)
- Definir el dominio puro base (ErrorKind, IDs, Value, SadConfig completo) con defaults SSOT y tests unit/prop.

## In (scope)
- S02-T01 a S02-T04.

## Out / No-goals del sprint
- No hay IO ni loader TOML.
- No hay decoders JSON.
- No hay bridge ni gateway.

## Dependencias
- Depende de: S01
- Bloquea: S03 (wrapper/ports), S05 (decoders), S06 (interpolacion).

## Gates (con pass/fail)
- S02-G1: make ci
  - Comando: make ci
  - Pass: `gleam format --check src test`, `gleam test`, `gleam test --coverage` en orden y sin errores
  - Fail: cualquier comando con exit code != 0

## Backlog (IDs)
- S02-T01 - Tipos base y errores (ErrorKind, Lifecycle, Value, IDs, secretos) + SadConfig completo
- S02-T02 - Tipos wire minimos para runner (RunnerEvent/RunnerResponse)
- S02-T03 - Helpers de tests (assertions y golden)
- S02-T04 - Tests unit y properties de roundtrip (qcheck 200)

## Config introducida/modificada
- SadConfig completo con defaults canonicos (ver docs/plan/limits.md)

## DX / Make targets
- Nuevos: -
- Actualizados: -

## Rollback rule (si un gate falla)
- Dia 3: si el gate principal no esta verde, se congela scope nuevo.
- Se prioriza pasar gates y DoD global.

## Tests (resumen)
- Archivos de test definidos en las historias del sprint.
- Los casos exactos estan en docs/plan/stories/*.md.
- `test/properties/types_prop_test.gleam`
- `test/unit/types_test.gleam`

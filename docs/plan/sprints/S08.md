# Sprint S08 - Bridge continuous + SSE upstream + managed_port

## Objetivo (1 frase)
- Continuous E2E con health check, HTTP client, SSE upstream y managed_port.

## In (scope)
- S08-T01 a S08-T05.

## Out / No-goals del sprint
- No hay core OTP ni gateway.
- No hay A2A/AG-UI/A2UI.

## Dependencias
- Depende de: S04, S06, S07
- Bloquea: S09 (StreamSink), S12 (Manager) y S14 (interact).

## Gates (con pass/fail)
- S08-G1: make e2e-continuous
  - Comando: make e2e-continuous
  - Pass: make wrapper + gleam test sin errores
  - Fail: cualquier comando con exit code != 0

## Backlog (IDs)
- S08-T01 - start_server continuous + health_check
- S08-T02 - HTTP client sync + SSE upstream
- S08-T03 - managed_port env injection + base_url
- S08-T04 - multipart (non-stream) + limites
- S08-T05 - Tests E2E continuous (echo_server, streaming, crasher)

## Config introducida/modificada
- limits.health_check_timeout_ms
- limits.max_http_response_bytes
- limits.max_runner_event_bytes
- limits.max_file_fetch_bytes
- network.managed_port_host

## DX / Make targets
- Nuevos: make e2e-continuous
- Actualizados: make e2e-echo, make e2e-transient

## Rollback rule (si un gate falla)
- Dia 3: si el gate principal no esta verde, se congela scope nuevo.
- Se prioriza pasar gates y DoD global.

## Tests (resumen)
- Archivos de test definidos en las historias del sprint.
- Los casos exactos estan en docs/plan/stories/*.md.
- `test/integration/client_test.gleam`
- `test/integration/managed_port_semantics_test.gleam`
- `test/integration/runner_test.gleam`

# S18-T02 - Stop a todas las instancias en shutdown

## Sprint
- Sprint: S18
- Capa: Core OTP
- Riesgo: Alto
- Gate: No

## SSOT (obligatorio)
- arquitectura/actores.md - shutdown_all y Terminate(NodeShuttingDown)
- arquitectura/tests.md - shutdown tests

## Inputs (obligatorio)
- OS/entorno:
  - Linux: Si
  - Docker CI: Si
  - Dependencias externas: None
- Config keys usadas (con defaults esperados):
  - limits.shutdown_timeout_ms (default 10000)
- Fixtures exactos:
  - Ninguna
- Supuestos:
  - shutdown usa Terminate(NodeShuttingDown), no StopInstance

## Outputs (obligatorio)
- Codigo:
  - src/saar/core/shutdown_all.gleam:
    - lista instancias via registry
    - envia terminate a cada agente
- Tests (inventario exacto):
  - test/integration/shutdown_test.gleam:
    - shutdown_sends_terminate_to_all_agents
- Make/targets:
  - Ninguno
- Artefactos runtime (si aplica):
  - Ninguno

## Limites y defaults (obligatorio)
- limits.shutdown_timeout_ms: 10000

## Acceptance Criteria (obligatorio)
- Casos correctos:
  - Todas las instancias reciben Terminate(NodeShuttingDown).
  - No se espera a que terminen interacciones (se abortan).
- Casos incorrectos:
  - Se usa StopInstance(UserRequested) en shutdown.

## Semantica de errores (obligatorio)
- ErrorKind esperado:
  - N/A
- ProblemDetails esperado:
  - N/A

## Evidence reproducible (obligatorio)
- Comandos:
  - gleam test
- Esperado:
  - Exit code 0
- Notas de auditoria:
  - Verificar que cancelacion inflight genera InteractionError cancelled

## No-goals (obligatorio)
  - Wait individual por agente

## Dependencias
- S12-T02

# S13-T03 - Loader profiles.sources (dir/git) con cache

## Sprint
- Sprint: S13
- Capa: Gateway
- Riesgo: Alto
- Gate: No

## SSOT (obligatorio)
- arquitectura/gateway.md - reload profiles (mantener set anterior)
- docs/plan/decisions.md - profiles.sources y runner resolution

## Inputs (obligatorio)
- OS/entorno:
  - Linux: Si
  - Docker CI: Si
  - Dependencias externas: git
- Config keys usadas (con defaults esperados):
  - profiles.sources
  - profiles.git_cache_dir
  - runners.python_bin
- Fixtures exactos:
  - test/fixtures/source_local/{profiles,runners}
- Supuestos:
  - Carga atomica: si algo falla, se conserva el set anterior
  - Resolucion de runner solo dentro del source

## Outputs (obligatorio)
- Codigo:
  - src/saar/profiles_sources.gleam:
    - dir source: scan <root>/profiles/*.json y <root>/runners
    - git source: clone/fetch y checkout ref
    - validacion de runner existe y es ejecutable
    - swap atomico de set completo
- Tests (inventario exacto):
  - test/integration/profiles_reload_test.gleam:
    - reload_dir_source_ok
    - reload_git_source_failure_keeps_previous
    - reload_missing_runner_fails
- Make/targets:
  - Ninguno
- Artefactos runtime (si aplica):
  - profiles.git_cache_dir

## Limites y defaults (obligatorio)
- N/A

## Acceptance Criteria (obligatorio)
- Casos correctos:
  - Reload aplica set nuevo solo si todo es valido.
  - Git source usa clone/fetch y checkout ref.
  - Runner se resuelve con prioridad: <root>/runners/<type> si existe y es ejecutable; si no, <root>/runners/<type>.py.
- Casos incorrectos:
  - Perfil invalido se ignora (debe fallar todo).
  - Runner missing no invalida reload (debe fallar).
  - Se usa runner fuera del source (no permitido).

## Semantica de errores (obligatorio)
- ErrorKind esperado:
  - N/A (gateway devuelve 500 en /sys/reload-profiles)
- ProblemDetails esperado:
  - N/A

## Evidence reproducible (obligatorio)
- Comandos:
  - gleam test
- Esperado:
  - Exit code 0
- Notas de auditoria:
  - Verificar que reload falla si falta runner

## No-goals (obligatorio)
  - Fallback a runners externos

## Dependencias
- S13-T02

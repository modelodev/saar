# S10-T02 - RegistryActor + API publica

## Sprint
- Sprint: S10
- Capa: Core OTP
- Riesgo: Medio
- Gate: No

## SSOT (obligatorio)
- arquitectura/tipos.md - RegistryMsg/RegistryError/InstanceKey
- arquitectura/actores.md - RegistryActor SSOT y monitoreo
- arquitectura/tests.md - registry tests

## Inputs (obligatorio)
- OS/entorno:
  - Linux: Si
  - Docker CI: Si
  - Dependencias externas: None
- Config keys usadas (con defaults esperados):
  - limits.registry_timeout_ms (default 5000)
- Fixtures exactos:
  - Ninguna
- Supuestos:
  - RegistryActor es SSOT de instancias activas

## Outputs (obligatorio)
- Codigo:
  - src/sad/core/registry.gleam
  - src/sad/core/registry_api.gleam:
    - register/lookup/list_all/list_by_profile/unregister
    - lookup_by_instance_id
    - usa safe_call.call_within con timeout explicito
- Tests (inventario exacto):
  - test/unit/registry_test.gleam:
    - register_new_instance
    - register_duplicate_fails
    - register_duplicate_instance_id_fails
    - register_is_atomic_for_uniqueness
    - unregister_removes
    - list_by_profile_filters
    - lookup_by_ids_uses_key
    - lookup_by_instance_id_returns_ref
    - agent_down_removes_entry
    - agent_down_removes_instance_id_index
    - monitor_established_on_register
- Make/targets:
  - Ninguno
- Artefactos runtime (si aplica):
  - Ninguno

## Limites y defaults (obligatorio)
- limits.registry_timeout_ms: 5000

## Acceptance Criteria (obligatorio)
- Casos correctos:
  - register es la fuente de unicidad (sin TOCTOU).
  - instance_id es unico globalmente (sin ambiguedad por perfil).
  - Un DOWN del agente elimina la entrada automaticamente.
  - unregister es idempotente.
- Casos incorrectos:
  - register permite duplicados.
  - lookup/list usan solo instance_id (sin profile_id) cuando es InstanceKey.

## Semantica de errores (obligatorio)
- ErrorKind esperado:
  - N/A (actor interno)
- ProblemDetails esperado:
  - N/A

## Evidence reproducible (obligatorio)
- Comandos:
  - gleam test
- Esperado:
  - Exit code 0
- Notas de auditoria:
  - Verificar uso de InstanceKey(profile_id, instance_id)

## No-goals (obligatorio)
  - Rehidratacion tras crash (v0 no persiste)

## Dependencias
- S09-T01
- S10-T01

# S14-T03 - POST /agents/:instance_id/interact streaming SSE

## Sprint
- Sprint: S14
- Capa: Gateway
- Riesgo: Alto
- Gate: No

## SSOT (obligatorio)
- arquitectura/gateway.md - streaming segun capability.streaming
- arquitectura/protocolos.md - AG-UI y A2UI nativo
- arquitectura/tests.md - post_agents_interact_streaming_a2ui_header_switches_wire

## Inputs (obligatorio)
- OS/entorno:
  - Linux: Si
  - Docker CI: Si
  - Dependencias externas: None
- Config keys usadas (con defaults esperados):
  - limits.sse_keep_alive_interval_ms (default 15000)
  - interaction_stream.push_timeout_ms (default 250)
- Fixtures exactos:
  - test/fixtures/source_local/profiles/*.json
- Supuestos:
  - capability.streaming = true => SSE siempre

## Outputs (obligatorio)
- Codigo:
  - src/sad/gateway/agents_api.gleam:
    - POST /agents/:instance_id/interact (SSE cuando streaming=true)
    - AG-UI por defecto
    - A2UI si header X-SAD-UI-Protocol: a2ui/v0.8
- Tests (inventario exacto):
  - test/integration/api_test.gleam:
    - post_agents_interact_streaming_a2ui_header_switches_wire
    - post_agents_interact_streaming_a2ui_disconnect_is_terminal
- Make/targets:
  - make e2e-http-interact
- Artefactos runtime (si aplica):
  - Ninguno

## Limites y defaults (obligatorio)
- limits.sse_keep_alive_interval_ms: 15000
- interaction_stream.push_timeout_ms: 250

## Acceptance Criteria (obligatorio)
- Casos correctos:
  - Si capability.streaming=true, la respuesta es SSE sin negociar.
  - AG-UI emite RUN_ERROR en fallo y cierra SSE.
  - A2UI nativo termina por cierre de conexion (sin evento terminal).
- Casos incorrectos:
  - SSE cancelando la ejecucion al desconectar.

## Semantica de errores (obligatorio)
- ErrorKind esperado:
  - agent_error para Busy (code "interact_while_busy")
  - infra_error para timeout y worker_down
- ProblemDetails esperado:
  - 404 si instance_id no existe

## Evidence reproducible (obligatorio)
- Comandos:
  - make e2e-http-interact
- Esperado:
  - Exit code 0
- Notas de auditoria:
  - Verificar formato SSE y keep-alive

## No-goals (obligatorio)
  - WebSockets (rechazados)

## Dependencias
- S13-T06
- S09-T02

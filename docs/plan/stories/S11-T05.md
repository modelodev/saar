# S11-T05 - API interna agent_internal

## Sprint
- Sprint: S11
- Capa: Core OTP
- Riesgo: Alto
- Gate: No

## SSOT (obligatorio)
- arquitectura/actores.md - separacion publico vs interno
- arquitectura/tests.md - worker_down y timeout handling

## Inputs (obligatorio)
- OS/entorno:
  - Linux: Si
  - Docker CI: Si
  - Dependencias externas: None
- Config keys usadas (con defaults esperados):
  - limits.call_timeout_ms (default 30000)
- Fixtures exactos:
  - Ninguna
- Supuestos:
  - Actor monitoriza worker_ref siempre

## Outputs (obligatorio)
- Codigo:
  - src/saar/core/agent_internal.gleam:
    - provisioning_done
    - ingest_log (WorkerLog)
    - interaction_done
    - worker_down
    - server_died
    - terminate
- Tests (inventario exacto):
  - test/unit/agent_test.gleam:
    - killing_worker_does_not_crash_actor
    - worker_down_without_done_is_handled
    - timeout_does_not_crash_actor
    - hard_timeout_not_extended_by_output
- Make/targets:
  - Ninguno
- Artefactos runtime (si aplica):
  - Ninguno

## Limites y defaults (obligatorio)
- limits.call_timeout_ms: 30000

## Acceptance Criteria (obligatorio)
- Casos correctos:
  - WorkerDown sin InteractionDone => InteractionError infra_error code "worker_down".
  - Timeout aborta worker y vuelve a Idle sin tumbar al actor; en transient se cierra el port y se detiene el runner, en continuous solo falla la llamada.
- Casos incorrectos:
  - WorkerDown tumba al actor o deja Busy sin limpiar.

## Semantica de errores (obligatorio)
- ErrorKind esperado:
  - infra_error con code "worker_down"
  - infra_error con code "timeout"
- ProblemDetails esperado:
  - N/A

## Evidence reproducible (obligatorio)
- Comandos:
  - gleam test
- Esperado:
  - Exit code 0
- Notas de auditoria:
  - Verificar monitor y limpieza de estado

## No-goals (obligatorio)
  - Reintentos automaticos del worker

## Dependencias
- S11-T01
- S11-T04

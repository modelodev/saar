# S17-T03 - serve -b/-k/--status + pid/log files

## Sprint
- Sprint: S17
- Capa: CLI
- Riesgo: Alto
- Gate: No

## SSOT (obligatorio)
- docs/arquitectura/operaciones.md - serve background/kill/status
- docs/arquitectura/tests.md - serve_* tests

## Inputs (obligatorio)
- OS/entorno:
  - Linux: Si
  - Docker CI: Si
  - Dependencias externas: None
- Config keys usadas (con defaults esperados):
  - limits.shutdown_timeout_ms (default 10000)
  - server.port (default 8080)
- Fixtures exactos:
  - Ninguna
- Supuestos:
  - pidfile/logfile default en ~/.sad (expandir ~)

## Outputs (obligatorio)
- Codigo:
  - src/sad/cli.gleam:
    - serve -b/--background
    - serve -k/--kill
    - serve --status
  - src/sad/daemon_paths.gleam:
    - resolve_pidfile_path
    - resolve_logfile_path
- Tests (inventario exacto):
  - test/integration/daemon_test.gleam:
    - serve_status_running
    - serve_status_not_running
- Make/targets:
  - Ninguno
- Artefactos runtime (si aplica):
  - ~/.sad/sad.pid
  - ~/.sad/sad.log

## Limites y defaults (obligatorio)
- limits.shutdown_timeout_ms: 10000
- server.port: 8080

## Acceptance Criteria (obligatorio)
- Casos correctos:
  - serve --status imprime PID y puerto si esta vivo.
  - serve --status limpia pidfile stale.
  - serve -k mata proceso y borra pidfile.
- Casos incorrectos:
  - serve --status requiere /health/ready.

## Semantica de errores (obligatorio)
- ErrorKind esperado:
  - N/A
- ProblemDetails esperado:
  - N/A

## Evidence reproducible (obligatorio)
- Comandos:
  - gleam test
- Esperado:
  - Exit code 0
- Notas de auditoria:
  - Verificar salida exacta: "SAD running on port <port> (PID <pid>)"

## No-goals (obligatorio)
  - Health check activo en serve --status

## Dependencias
- S17-T02

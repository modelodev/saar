# S01-T06 - Doc-lint minimo (docs/snippets) como test Gleam

## Sprint
- Sprint: S01
- Capa: Docs
- Riesgo: Bajo
- Gate: No

## SSOT (obligatorio)
- docs/plan/README.md - Plan de ejecucion (doc-lint)
- arquitectura/tests.md - doc-lint minimo

## Inputs (obligatorio)
- OS/entorno:
  - Linux: Si
  - Docker CI: Si
  - Dependencias externas: None
- Config keys usadas (con defaults esperados):
  - Ninguna
- Fixtures exactos:
  - arquitectura/**/*.md
  - arquitectura/examples/snippets/**/*
  - docs/plan/limits.toml
- Supuestos:
  - Doc-lint se implementa como test Gleam (gleeunit)

## Outputs (obligatorio)
- Codigo:
  - test/unit/doc_lint_test.gleam
- Tests (inventario exacto):
  - doc_lint_forbidden_patterns
  - doc_lint_actor_stop_usage
  - doc_lint_limits_defaults_match_config
  - doc_lint_limits_md_matches_toml
  - doc_lint_config_defaults_match_config
- Make/targets:
  - Ninguno (corre dentro de `gleam test`)
- Artefactos runtime (si aplica):
  - Ninguno

## Limites y defaults (obligatorio)
- N/A

## Acceptance Criteria (obligatorio)
- Casos correctos:
  - `gleam test` pasa con docs actuales.
  - Cuando aparece un patron prohibido, el test falla con mensaje `DOC_LINT_FAIL file=<path> pattern=<pattern>`.
  - Defaults en `docs/plan/limits.toml` y `docs/arquitectura/config.md` coinciden con `SadConfig.default_*`.
  - `docs/plan/limits.md` se mantiene en sync con `docs/plan/limits.toml`.
- Casos incorrectos:
  - Doc-lint no detecta un patron prohibido.
  - El mensaje de error no incluye archivo y patron.

## Semantica de errores (obligatorio)
- ErrorKind esperado:
  - N/A
- ProblemDetails esperado:
  - N/A

## Evidence reproducible (obligatorio)
- Comandos:
  - gleam test
- Esperado:
  - Exit code 0 con docs limpios
- Notas de auditoria:
  - Regla 1: prohibir patrones `process.try_call`, `process.select_after`, `process.select(selector)`
  - Regla 2: prohibir `actor.stop(process.Normal)` en docs/snippets
  - Regla 3: defaults docs ↔ config (SSOT)
  - Regla 4: limits.md generado ↔ limits.toml

## No-goals (obligatorio)
- Arch-lint sobre src/ (se evalua mas adelante).
- Validar wrapper stdout (S03, test de integracion).

## Dependencias
- S01-T01

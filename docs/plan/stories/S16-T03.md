# S16-T03 - Adapter A2A (decode/encode + agent-card)

## Sprint
- Sprint: S16
- Capa: Adapters
- Riesgo: Alto
- Gate: No

## SSOT (obligatorio)
- arquitectura/protocolos.md - A2A wire y errores
- arquitectura/gateway.md - agent-card auth
- arquitectura/tests.md - a2a_test.gleam

## Inputs (obligatorio)
- OS/entorno:
  - Linux: Si
  - Docker CI: Si
  - Dependencias externas: None
- Config keys usadas (con defaults esperados):
  - auth.api_key (required)
- Fixtures exactos:
  - Ninguna
- Supuestos:
  - task_id == trace_id
  - A2A file.bytes se rechaza

## Outputs (obligatorio)
- Codigo:
  - src/sad/adapters/a2a.gleam:
    - decode/encode Message/Task/Parts
    - error mapping A2A
    - agent_card_from_instance(info, profile_meta)
- Tests (inventario exacto):
  - test/unit/a2a_test.gleam:
    - message_to_payload_text_only
    - message_to_payload_files_only
    - message_to_payload_mixed
    - message_to_payload_empty
    - interaction_result_to_task
    - agent_card_uses_meta_id
    - sad_error_to_a2a_error
    - to_sse_line_format
    - decode_a2a_message_missing_parts
    - decode_a2a_message_invalid_role
    - decode_a2a_task_invalid_id
    - decode_a2a_part_unknown_ignored
    - decode_a2a_file_bytes_rejected
    - decode_a2a_file_media_type_optional
    - decode_a2a_text_parts_concatenated
    - a2a_message_send_shape_exact
    - a2a_stream_success_sequence_exact
    - a2a_stream_error_payload_exact
    - a2ui_data_part_requires_extension
    - a2ui_stream_message_shape_exact
    - validate_agent_card_missing_name
    - validate_agent_card_missing_url
- Make/targets:
  - Ninguno
- Artefactos runtime (si aplica):
  - Ninguno

## Limites y defaults (obligatorio)
- N/A

## Acceptance Criteria (obligatorio)
- Casos correctos:
  - Agent Card se deriva de la instancia (no de perfil global).
  - task_id == trace_id en send y stream.
  - file.bytes se rechaza con BadRequest.
- Casos incorrectos:
  - Agent Card publico sin auth.

## Semantica de errores (obligatorio)
- ErrorKind esperado:
  - A2A errors usan type A2A (no SAD)
- ProblemDetails esperado:
  - 401 sin token

## Evidence reproducible (obligatorio)
- Comandos:
  - gleam test
- Esperado:
  - Exit code 0
- Notas de auditoria:
  - Verificar task_id == trace_id

## No-goals (obligatorio)
  - Versionado A2A v1 HTTP binding completo

## Dependencias
- S13-T04
- S14-T01

# S18-T01 - Drain HTTP server en shutdown

## Sprint
- Sprint: S18
- Capa: Gateway
- Riesgo: Alto
- Gate: No

## SSOT (obligatorio)
- arquitectura/gateway.md - shutdown y drain
- arquitectura/operaciones.md - SIGTERM handler

## Inputs (obligatorio)
- OS/entorno:
  - Linux: Si
  - Docker CI: Si
  - Dependencias externas: None
- Config keys usadas (con defaults esperados):
  - limits.shutdown_timeout_ms (default 10000)
- Fixtures exactos:
  - Ninguna
- Supuestos:
  - SIGTERM y serve -k comparten flujo de shutdown

## Outputs (obligatorio)
- Codigo:
  - src/sad/gateway/shutdown.gleam:
    - inicia drain (deja de aceptar)
    - rechaza nuevas requests con 503 code "shutting_down"
- Tests (inventario exacto):
  - test/integration/shutdown_test.gleam:
    - shutdown_completes_inflight_request
    - shutdown_rejects_new_requests
- Make/targets:
  - Ninguno
- Artefactos runtime (si aplica):
  - Ninguno

## Limites y defaults (obligatorio)
- limits.shutdown_timeout_ms: 10000

## Acceptance Criteria (obligatorio)
- Casos correctos:
  - Nuevas requests reciben 503 shutting_down.
  - In-flight se completan best-effort hasta timeout.
- Casos incorrectos:
  - Se siguen aceptando conexiones tras iniciar shutdown.

## Semantica de errores (obligatorio)
- ErrorKind esperado:
  - infra_error con code "shutting_down"
- ProblemDetails esperado:
  - 503 con type SAD

## Evidence reproducible (obligatorio)
- Comandos:
  - gleam test
- Esperado:
  - Exit code 0
- Notas de auditoria:
  - Verificar que SIGTERM usa el mismo flujo que serve -k

## No-goals (obligatorio)
  - Persistencia de in-flight tras restart

## Dependencias
- S17-T03

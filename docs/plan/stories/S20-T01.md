# S20-T01 - Integrar Landlock best-effort

## Sprint
- Sprint: S20
- Capa: Wrapper
- Riesgo: Alto
- Gate: No

## SSOT (obligatorio)
- docs/plan/decisions.md - landlock policy
- docs/arquitectura/examples/wrapper_pid_namespace.md - wrapper isolation

## Inputs (obligatorio)
- OS/entorno:
  - Linux: Si (Landlock requiere kernel >= 5.13)
  - Docker CI: Si (best_effort puede skip)
  - Dependencias externas: None
- Config keys usadas (con defaults esperados):
  - security.landlock_mode (default best_effort)
- Fixtures exactos:
  - Ninguna
- Supuestos:
  - enforced falla solo la instancia, no el SAD

## Outputs (obligatorio)
- Codigo:
  - native/wrapper/sad_wrapper.c:
    - aplicar Landlock segun security.landlock_mode
    - scope v0 (workspace write, runners/profiles read, /proc/self read)
- Tests (inventario exacto):
  - test/integration/landlock_test.gleam:
    - landlock_best_effort_does_not_break_without_support
    - landlock_enforced_fails_instance_if_unavailable (skip si no soporta)
- Make/targets:
  - Ninguno
- Artefactos runtime (si aplica):
  - Ninguno

## Limites y defaults (obligatorio)
- security.landlock_mode: best_effort

## Acceptance Criteria (obligatorio)
- Casos correctos:
  - best_effort: si no soporta, continua sin Landlock (DEBUG=1 log).
  - enforced: si no soporta, falla la instancia con LANDLOCK_UNAVAILABLE.
- Casos incorrectos:
  - enforced tumba el proceso SAD completo.

## Semantica de errores (obligatorio)
- ErrorKind esperado:
  - infra_error en interaccion/provisioning (LANDLOCK_UNAVAILABLE)
- ProblemDetails esperado:
  - N/A

## Evidence reproducible (obligatorio)
- Comandos:
  - gleam test
- Esperado:
  - Exit code 0
- Notas de auditoria:
  - Verificar que el wrapper no escribe en STDOUT

## No-goals (obligatorio)
  - Allowlist de paths extra (post-v0)

## Dependencias
- S03-T01

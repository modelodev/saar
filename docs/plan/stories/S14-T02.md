# S14-T02 - POST /agents/:instance_id/interact sync

## Sprint
- Sprint: S14
- Capa: Gateway
- Riesgo: Alto
- Gate: No

## SSOT (obligatorio)
- docs/arquitectura/gateway.md - POST /agents/:instance_id/interact
- docs/arquitectura/protocolos.md - trace_id y errores
- docs/arquitectura/tests.md - post_agents_interact_transient/continuous

## Inputs (obligatorio)
- OS/entorno:
  - Linux: Si
  - Docker CI: Si
  - Dependencias externas: None
- Config keys usadas (con defaults esperados):
  - limits.call_timeout_ms (default 30000)
  - limits.max_request_body_bytes (default 1048576)
- Fixtures exactos:
  - test/fixtures/source_local/profiles/*.json
  - test/fixtures/payloads/*.json
- Supuestos:
  - capability.streaming = false => respuesta JSON sync

## Outputs (obligatorio)
- Codigo:
  - src/sad/gateway/agents_api.gleam:
    - POST /agents/:instance_id/interact (sync cuando streaming=false)
    - body: {capability, inputs, context.trace_id?}
    - trace_id opcional (si falta, UUIDv7)
- Tests (inventario exacto):
  - test/integration/api_test.gleam:
    - post_agents_interact_transient
    - post_agents_interact_continuous
    - post_agents_interact_timeout
    - post_agents_interact_timeout_then_next_request_ok
- Make/targets:
  - make e2e-http-interact
- Artefactos runtime (si aplica):
  - Ninguno

## Limites y defaults (obligatorio)
- limits.call_timeout_ms: 30000
- limits.max_request_body_bytes: 1048576

## Acceptance Criteria (obligatorio)
- Casos correctos:
  - Request minimo: {capability, inputs, context?}.
  - trace_id se genera si falta y se devuelve en respuesta.
  - Si capability.streaming=false, siempre JSON (ignora headers UI).
- Casos incorrectos:
  - stream en body (no existe).
  - capability faltante o inputs invalidos -> 400.

## Semantica de errores (obligatorio)
- ErrorKind esperado:
  - agent_error con code "interact_while_busy" -> 422
  - infra_error con code "timeout" -> 504
- ProblemDetails esperado:
  - 404 si instance_id no existe

## Evidence reproducible (obligatorio)
- Comandos:
  - make e2e-http-interact
- Esperado:
  - Exit code 0
- Notas de auditoria:
  - Verificar que el timeout efectivo viene del actor

## No-goals (obligatorio)
  - Seleccion de streaming por request

## Dependencias
- S13-T06
- S11-T04

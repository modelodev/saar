# S12-T02 - AgentManagerActor create/stop/start/delete/list

## Sprint
- Sprint: S12
- Capa: Core OTP
- Riesgo: Alto
- Gate: No

## SSOT (obligatorio)
- arquitectura/actores.md - AgentManagerActor, orden StartAgent
- arquitectura/tipos.md - AgentManagerMsg/StartArgs/StartError/StopError/DeleteError
- arquitectura/tests.md - start_agent_* tests

## Inputs (obligatorio)
- OS/entorno:
  - Linux: Si
  - Docker CI: Si
  - Dependencias externas: None
- Config keys usadas (con defaults esperados):
  - limits.registry_timeout_ms (default 5000)
  - limits.call_timeout_ms (default 30000)
- Fixtures exactos:
  - Ninguna
- Supuestos:
  - StartArgs ya contiene profile snapshot y params resueltos
  - instance_id es unico globalmente (registry)
  - Avoid semantic duplication: do not re-implement checked port allocation retries in AgentManager; delegate to `saar/core/port_pool_actor` (SSOT) and/or `saar/port_pool/checked`.
  - Avoid overengineering: keep Actor API wrappers explicit (no generic utils module) unless duplication is proven across multiple actors.

## Outputs (obligatorio)
- Codigo:
  - src/saar/core/agent_manager.gleam
  - src/saar/core/agent_manager_api.gleam:
    - start_agent/stop_agent/delete_agent/list_agents
    - usa safe_call.call_within y call_result_within con timeouts explicitos
- Tests (inventario exacto):
  - test/unit/agent_manager_test.gleam:
    - start_agent_same_key_one_wins
    - start_agent_registration_failed_rolls_back
- Make/targets:
  - Ninguno
- Artefactos runtime (si aplica):
  - Ninguno

## Limites y defaults (obligatorio)
- limits.registry_timeout_ms: 5000
- limits.call_timeout_ms: 30000

## Acceptance Criteria (obligatorio)
- Casos correctos:
  - StartAgent retorna Ok tras registrar (provisioning async).
  - Orden: spawn -> register -> si falla, terminate y error AlreadyExists.
  - Un mismo instance_id con otro profile_id tambien falla (AlreadyExists).
  - Stop/Delete sobre instancia inexistente son Ok (idempotente).
- Casos incorrectos:
  - StartAgent hace lookup previo (TOCTOU).
  - Manager mantiene indice propio.

## Semantica de errores (obligatorio)
- ErrorKind esperado:
  - StartError.AlreadyExists (code "already_exists")
- ProblemDetails esperado:
  - N/A (actor interno)

## Evidence reproducible (obligatorio)
- Comandos:
  - gleam test
- Esperado:
  - Exit code 0
- Notas de auditoria:
  - Verificar rollback del agente creado cuando registry.register falla
  - Verificar unicidad global por instance_id

## No-goals (obligatorio)
  - Esperar provisioning en StartAgent

## Dependencias
- S10-T02
- S12-T01

# S07-T01 - Modulo port_pool allocate/release

## Sprint
- Sprint: S07
- Capa: Shared
- Riesgo: Medio
- Gate: No

## SSOT (obligatorio)
- arquitectura/examples/snippets/port_pool.gleam - API y errores
- arquitectura/tests.md - port_pool tests

## Inputs (obligatorio)
- OS/entorno:
  - Linux: Si
  - Docker CI: Si
  - Dependencias externas: None
- Config keys usadas (con defaults esperados):
  - limits.port_range_min (default 9000)
  - limits.port_range_max (default 9999)
- Fixtures exactos:
  - Ninguna
- Supuestos:
  - allocate es determinista (menor puerto libre)
  - allocate idempotente por instance_id
  - allocate_checked valida disponibilidad real via bind-check

## Outputs (obligatorio)
- Codigo:
  - src/sad/port_pool.gleam:
    - PortPool
    - PortPoolError (InvalidRange, PoolExhausted, PortInUse, BindCheckFailed, NoAvailablePortAfterRetries)
    - PortCheckError (CheckPortInUse, CheckBindFailed)
    - init(min, max)
    - allocate(pool, instance_id)
    - allocate_checked(pool, instance_id, check)
    - release(pool, instance_id)
- Tests (inventario exacto):
  - test/unit/port_pool_test.gleam:
    - port_pool_allocate_returns_port
    - port_pool_allocate_checked_returns_port
    - port_pool_allocate_checked_skips_in_use
    - port_pool_allocate_checked_port_in_use_error
    - port_pool_allocate_checked_no_available_after_retries
    - port_pool_allocate_checked_bind_check_failed
    - port_pool_allocate_checked_pool_exhausted
    - port_pool_allocate_unique
    - port_pool_release_frees_port
    - port_pool_reuse_after_release
    - port_pool_respects_range
    - port_pool_requires_explicit_range
- Make/targets:
  - Ninguno
- Artefactos runtime (si aplica):
  - Ninguno

## Limites y defaults (obligatorio)
- limits.port_range_min: 9000
- limits.port_range_max: 9999

## Acceptance Criteria (obligatorio)
- Casos correctos:
  - allocate devuelve el menor puerto libre disponible.
  - allocate es idempotente por instance_id.
  - allocate_checked intenta bind-checks y salta puertos ocupados.
  - release libera el puerto inmediatamente.
- Casos incorrectos:
  - min <= 0 o max < min => Error(InvalidRange).

## Semantica de errores (obligatorio)
- ErrorKind esperado:
  - N/A (helper puro)
- ProblemDetails esperado:
  - N/A

## Evidence reproducible (obligatorio)
- Comandos:
  - gleam test
- Esperado:
  - Exit code 0
- Notas de auditoria:
  - Verificar que allocate usa el menor puerto libre

## No-goals (obligatorio)
- PortPoolActor (S10)
- Provisioning OTP (S12)

## Dependencias
- S02-T01

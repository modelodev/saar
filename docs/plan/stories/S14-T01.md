# S14-T01 - GET /agents/:instance_id (info + capabilities)

## Sprint
- Sprint: S14
- Capa: Gateway
- Riesgo: Medio
- Gate: No

## SSOT (obligatorio)
- docs/arquitectura/gateway.md - GET /agents/:instance_id info y capabilities
- docs/arquitectura/tests.md - get_agent_capabilities

## Inputs (obligatorio)
- OS/entorno:
  - Linux: Si
  - Docker CI: Si
  - Dependencias externas: None
- Config keys usadas (con defaults esperados):
  - limits.call_timeout_ms (default 30000)
- Fixtures exactos:
  - test/fixtures/source_local/profiles/*.json
- Supuestos:
  - :id es InstanceId y es unico globalmente

## Outputs (obligatorio)
- Codigo:
  - src/sad/gateway/agents_api.gleam:
    - GET /agents/:instance_id -> AgentInfoView + capabilities
- Tests (inventario exacto):
  - test/integration/api_test.gleam:
    - get_agent_capabilities
- Make/targets:
  - Ninguno
- Artefactos runtime (si aplica):
  - Ninguno

## Limites y defaults (obligatorio)
- limits.call_timeout_ms: 30000

## Acceptance Criteria (obligatorio)
- Casos correctos:
  - Respuesta incluye instance_id, profile_id, capabilities y a2a_base_url.
- Casos incorrectos:
  - Se expone el perfil completo o params resueltos.

## Semantica de errores (obligatorio)
- ErrorKind esperado:
  - agent_error si instancia en estado invalido
- ProblemDetails esperado:
  - 404 si instance_id no existe
  - N/A

## Evidence reproducible (obligatorio)
- Comandos:
  - gleam test
- Esperado:
  - Exit code 0
- Notas de auditoria:
  - Verificar que capabilities provienen del profile snapshot

## No-goals (obligatorio)
  - Endpoint /agents/:instance_id/capabilities separado

## Dependencias
- S13-T06

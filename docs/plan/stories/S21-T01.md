
## Sprint
- Sprint: S21
- Capa: Profiles
- Riesgo: Medio
- Gate: No

## SSOT (obligatorio)
- `docs/arquitectura/gateway.md` - API nativa y superficies
- `docs/arquitectura/integracion.md` - integración por perfil
- `src/sad/types/profile.gleam` - tipos de capability
- `src/sad/decoders.gleam` - decoders de perfil

## Inputs (obligatorio)
- Config keys usadas (con defaults esperados): Ninguna
- Fixtures exactos:
  - `docs/arquitectura/examples/profiles/aider/aider.json`
  - `docs/arquitectura/examples/profiles/lightrag/lightrag.json`
- Supuestos:
  - El modo de entrega de respuesta se declara por capability, no por agente.
  - Si el campo no existe, el comportamiento es el actual.

## Outputs (obligatorio)
- Diseño (perfil):
  - Añadir un campo opcional por capability (runner y http): `response_mode`
  - Valores: `sync` | `stream` | `deferred`
  - Defaults:
    - si falta `response_mode`, se asume `sync`
  - Validaciones:
    - si `response_mode=stream` entonces `streaming` debe ser `true`
    - si `response_mode=deferred` entonces `streaming` debe ser `false` (v1: diferido es resultado final por polling/suscripción)
- Código:
  - Actualizar `src/sad/types/profile.gleam` para incluir el campo y el tipo
  - Actualizar `src/sad/decoders.gleam` para decodificar y validar
- Docs:
  - Actualizar `INTEGRATION.md` (contrato de cliente)
  - Actualizar `runners_and_agents.md` (contrato de perfil/runner)
- Tests (inventario exacto):
  - `test/unit/decoders_test.gleam`:
    - `response_mode_defaults_to_sync`
    - `response_mode_stream_requires_streaming_true`
    - `response_mode_deferred_requires_streaming_false`
    - `response_mode_invalid_value_rejected`

## Límites y defaults (obligatorio)
- N/A

## Acceptance Criteria (obligatorio)
- Casos correctos:
  - Perfiles existentes siguen cargando sin cambios.
  - `response_mode` se expone en el view de capabilities (para clientes).
- Casos incorrectos:
  - Aceptar `response_mode=stream` con `streaming=false`.

## Semántica de errores (obligatorio)
- ErrorKind esperado:
  - `bad_request` en carga/validación de perfil inválido.

## Evidence reproducible (obligatorio)
- Comandos:
  - `gleam test`
- Esperado:
  - Exit code 0

## No-goals (obligatorio)
- No se añade cola ni planificación.

## Dependencias
- Ninguna

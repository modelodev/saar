# S11-T03 - Ring buffer de logs y attach con takeover

## Sprint
- Sprint: S11
- Capa: Core OTP
- Riesgo: Medio
- Gate: No

## SSOT (obligatorio)
- sad/arquitectura/actores.md - ring buffer y takeover
- sad/arquitectura/tests.md - logs ring buffer y attach_logs

## Inputs (obligatorio)
- OS/entorno:
  - Linux: Si
  - Docker CI: Si
  - Dependencias externas: None
- Config keys usadas (con defaults esperados):
  - limits.log_buffer_bytes (default 1048576)
- Fixtures exactos:
  - Ninguna
- Supuestos:
  - event_bytes = byte_size(utf8(message))

## Outputs (obligatorio)
- Codigo:
  - src/sad/core/agent.gleam:
    - LogBuffer (Deque + total_bytes)
    - add_log_event con drop oldest-first
    - oversize -> insertar "[log dropped: too large]"
    - attach_logs takeover unico (replay + live)
- Tests (inventario exacto):
  - test/unit/agent_test.gleam:
    - logs_ring_buffer_capacity
    - logs_ring_buffer_drops_oldest
    - logs_attach_sends_history
    - logs_attach_preserves_metadata
    - attach_logs_takeover
    - attach_logs_receives_events
- Make/targets:
  - Ninguno
- Artefactos runtime (si aplica):
  - Ninguno

## Limites y defaults (obligatorio)
- limits.log_buffer_bytes: 1048576

## Acceptance Criteria (obligatorio)
- Casos correctos:
  - Buffer limita por bytes del message (no cuenta metadata).
  - Replay envia historico en orden cronologico (oldest -> newest).
  - Takeover reemplaza suscriptor anterior.
- Casos incorrectos:
  - Overflow rompe UTF-8 o bloquea el actor.

## Semantica de errores (obligatorio)
- ErrorKind esperado:
  - N/A
- ProblemDetails esperado:
  - N/A

## Evidence reproducible (obligatorio)
- Comandos:
  - gleam test
- Esperado:
  - Exit code 0
- Notas de auditoria:
  - Verificar insercion del log sintetico en oversize

## No-goals (obligatorio)
  - Multiples suscriptores concurrentes

## Dependencias
- S11-T01

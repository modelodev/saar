# S13-T02 - Loader TOML con interpolacion de env

## Sprint
- Sprint: S13
- Capa: Gateway
- Riesgo: Alto
- Gate: No

## SSOT (obligatorio)
- sad/arquitectura/gateway.md - config loader y auth
- sad/arquitectura/operaciones.md - SAD_CONFIG_PATH y CLI config
- docs/plan/decisions.md - interpolacion solo ${VAR}

## Inputs (obligatorio)
- OS/entorno:
  - Linux: Si
  - Docker CI: Si
  - Dependencias externas: None
- Config keys usadas (con defaults esperados):
  - auth.api_key (required)
  - profiles.sources
- Fixtures exactos:
  - test/fixtures/config/test_config.toml
- Supuestos:
  - Interpolacion solo ${VAR}; si falta, error

## Outputs (obligatorio)
- Codigo:
  - src/sad/config_loader.gleam:
    - resolve_config_path(--config > SAD_CONFIG_PATH > ./config.toml)
    - parse TOML estricta (keys desconocidas error)
    - interpolacion ${VAR} solo en config.toml
    - auth.api_key requerido (startup fail si falta)
- Tests (inventario exacto):
  - test/integration/config_test.gleam:
    - env_interpolation_works
    - missing_env_var_fails
    - unknown_key_fails
    - missing_config_file_fails
    - missing_api_key_fails
- Make/targets:
  - Ninguno
- Artefactos runtime (si aplica):
  - Ninguno

## Limites y defaults (obligatorio)
- Ver docs/plan/limits.md

## Acceptance Criteria (obligatorio)
- Casos correctos:
  - Path resolve: --config > SAD_CONFIG_PATH > ./config.toml.
  - Defaults se aplican a keys omitidas.
- Casos incorrectos:
  - Key desconocida en TOML se ignora (debe fallar).
  - ${VAR} faltante se deja literal (debe fallar).

## Semantica de errores (obligatorio)
- ErrorKind esperado:
  - N/A (startup/reload error)
- ProblemDetails esperado:
  - N/A

## Evidence reproducible (obligatorio)
- Comandos:
  - gleam test
- Esperado:
  - Exit code 0
- Notas de auditoria:
  - Verificar error claro si config no existe

## No-goals (obligatorio)
  - Interpolacion {{env.VAR}}

## Dependencias
- S13-T01

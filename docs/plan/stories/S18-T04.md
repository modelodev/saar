# S18-T04 - Tests de shutdown y daemon

## Sprint
- Sprint: S18
- Capa: Gateway
- Riesgo: Medio
- Gate: No

## SSOT (obligatorio)
- arquitectura/tests.md - shutdown tests

## Inputs (obligatorio)
- OS/entorno:
  - Linux: Si
  - Docker CI: Si
  - Dependencias externas: None
- Config keys usadas (con defaults esperados):
  - limits.shutdown_timeout_ms (default 10000)
- Fixtures exactos:
  - Ninguna
- Supuestos:
  - serve -k usa el mismo flujo que SIGTERM

## Outputs (obligatorio)
- Codigo:
  - test/integration/shutdown_test.gleam
- Tests (inventario exacto):
  - shutdown_completes_inflight_request
  - shutdown_rejects_new_requests
  - shutdown_sends_terminate_to_all_agents
  - shutdown_force_kills_after_timeout
- Make/targets:
  - Ninguno
- Artefactos runtime (si aplica):
  - ~/.saar/saar.pid

## Limites y defaults (obligatorio)
- limits.shutdown_timeout_ms: 10000

## Acceptance Criteria (obligatorio)
- Casos correctos:
  - serve -k retorna 0 si no hay proceso o si se detuvo.
  - pidfile se elimina al finalizar shutdown.
- Casos incorrectos:
  - Shutdown no rechaza nuevas requests.

## Semantica de errores (obligatorio)
- ErrorKind esperado:
  - N/A
- ProblemDetails esperado:
  - N/A

## Evidence reproducible (obligatorio)
- Comandos:
  - gleam test
- Esperado:
  - Exit code 0
- Notas de auditoria:
  - Verificar salida "SAAR not running" en serve --status con pidfile stale

## No-goals (obligatorio)
  - Persistencia del registry

## Dependencias
- S18-T01
- S18-T02
- S18-T03

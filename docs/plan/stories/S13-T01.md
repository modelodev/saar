# S13-T01 - SadConfig actualizado (sources, limites, host/port)

## Sprint
- Sprint: S13
- Capa: Gateway
- Riesgo: Medio
- Gate: No

## SSOT (obligatorio)
- arquitectura/config.md - tabla de config y defaults
- arquitectura/examples/config/example-config.toml - defaults canonicos
- docs/plan/limits.toml - defaults del plan v2 (SSOT)

## Inputs (obligatorio)
- OS/entorno:
  - Linux: Si
  - Docker CI: Si
  - Dependencias externas: None
- Config keys usadas (con defaults esperados):
  - server.host (default 0.0.0.0)
  - server.port (default 8080)
  - auth.api_key (required)
  - profiles.sources
  - profiles.git_cache_dir
  - runners.python_bin
  - workspaces.directory (default ./workspaces)
  - limits.max_request_body_bytes
- Fixtures exactos:
  - test/fixtures/config/test_config.toml (si aplica)
- Supuestos:
  - SadConfig ya existe desde S02 (solo uso)

## Outputs (obligatorio)
- Codigo:
  - src/saar/types.gleam (uso de SadConfig en gateway)
- Tests (inventario exacto):
  - test/unit/types_test.gleam (default_config_invariants)
- Make/targets:
  - Ninguno
- Artefactos runtime (si aplica):
  - Ninguno

## Limites y defaults (obligatorio)
- Ver docs/plan/limits.toml

## Acceptance Criteria (obligatorio)
- Casos correctos:
  - Defaults coherentes con docs/plan/limits.toml.
- Casos incorrectos:
  - Key renombrada o fuera del esquema canonic.

## Semantica de errores (obligatorio)
- ErrorKind esperado:
  - N/A
- ProblemDetails esperado:
  - N/A

## Evidence reproducible (obligatorio)
- Comandos:
  - gleam test
- Esperado:
  - Exit code 0
- Notas de auditoria:
  - Verificar que el loader usara estos nombres exactos

## No-goals (obligatorio)
  - Cargar TOML (S13-T02)

## Dependencias
- S02-T01

# S02-T01 - Tipos base y errores (ErrorKind, Lifecycle, Value, IDs, secretos) + SadConfig completo

## Sprint
- Sprint: S02
- Capa: Shared
- Riesgo: Bajo
- Gate: No

## SSOT (obligatorio)
- arquitectura/examples/snippets/types.gleam - tipos base, ErrorKind, SecretVal, SadConfig
- arquitectura/examples/config/example-config.toml - defaults canonicos
- arquitectura/config.md - tabla de limites y streams

## Inputs (obligatorio)
- OS/entorno:
  - Linux: Si
  - Docker CI: Si
  - Dependencias externas: None
- Config keys usadas (con defaults esperados):
  - Ver docs/plan/limits.toml (SSOT; limits.md generado)
- Fixtures exactos:
  - Ninguna
- Supuestos:
  - Modulo puro (sin IO/OTP/Mist/Ports)
  - InstanceId es slug ASCII `[A-Za-z0-9_-]`, longitud 1..64

## Outputs (obligatorio)
- Codigo:
  - src/saar/types.gleam:
    - ErrorKind (bad_request, agent_error, infra_error)
    - SecretVal redactado (***REDACTED***)
    - IDs opacos ProfileId/InstanceId/TraceId + helpers (InstanceId valida y retorna Result)
    - InstanceIdError
    - Lifecycle
    - Value ADT (StringVal, IntVal, FloatVal, BoolVal, ListVal)
    - ValueType + helpers value_to_string/value_type
    - SadConfig completo + default_saar_config()
- Tests (inventario exacto):
  - test/unit/types_test.gleam:
    - error_kind_roundtrip
    - lifecycle_roundtrip
    - secret_redaction
    - id_roundtrip
    - instance_id_validation
    - default_config_invariants
- Make/targets:
  - Ninguno
- Artefactos runtime (si aplica):
  - Ninguno

## Limites y defaults (obligatorio)
- Ver docs/plan/limits.toml

## Acceptance Criteria (obligatorio)
- Casos correctos:
  - ErrorKind.to_string exacto: bad_request, agent_error, infra_error.
  - SecretVal se inspecciona como ***REDACTED***.
  - default_saar_config() coincide con defaults canonicos.
- Casos incorrectos:
  - ErrorKind string distinto a SSOT.
  - Redaccion distinta de ***REDACTED***.
  - Default de SadConfig fuera de tabla canonica.
  - InstanceId invalido (vacio, caracteres invalidos, longitud > 64).

## Semantica de errores (obligatorio)
- ErrorKind esperado:
  - N/A (puro)
- ProblemDetails esperado:
  - N/A

## Evidence reproducible (obligatorio)
- Comandos:
  - gleam test
- Esperado:
  - Exit code 0
- Notas de auditoria:
  - Verificar default_saar_config() vs docs/plan/limits.toml

## No-goals (obligatorio)
- Conversion Value <-> JSON (S04/S05).
- Loader TOML (S13).

## Dependencias
- S01-T01

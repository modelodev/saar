# S02-T01 - Tipos base y errores (ErrorKind, Lifecycle, Value, IDs, secretos) + SadConfig completo

## Sprint
- Sprint: S02
- Capa: Shared
- Riesgo: Bajo
- Gate: No

## SSOT (obligatorio)
- sad/arquitectura/examples/snippets/types.gleam - tipos base, ErrorKind, SecretVal, SadConfig
- sad/arquitectura/examples/config/example-config.toml - defaults canonicos
- sad/arquitectura/config.md - tabla de limites y streams

## Inputs (obligatorio)
- OS/entorno:
  - Linux: Si
  - Docker CI: Si
  - Dependencias externas: None
- Config keys usadas (con defaults esperados):
  - Ver docs/plan/limits.md (todas las keys)
- Fixtures exactos:
  - Ninguna
- Supuestos:
  - Modulo puro (sin IO/OTP/Mist/Ports)

## Outputs (obligatorio)
- Codigo:
  - src/sad/types.gleam:
    - ErrorKind (bad_request, agent_error, infra_error)
    - SecretVal redactado (***REDACTED***)
    - IDs opacos ProfileId/InstanceId/TraceId + helpers
    - Lifecycle
    - Value ADT (StringVal, IntVal, FloatVal, BoolVal, ListVal)
    - ValueType + helpers value_to_string/value_type
    - SadConfig completo + default_sad_config()
- Tests (inventario exacto):
  - test/unit/types_test.gleam:
    - error_kind_roundtrip
    - lifecycle_roundtrip
    - secret_redaction
    - id_roundtrip
    - default_config_invariants
- Make/targets:
  - Ninguno
- Artefactos runtime (si aplica):
  - Ninguno

## Limites y defaults (obligatorio)
- Ver docs/plan/limits.md

## Acceptance Criteria (obligatorio)
- Casos correctos:
  - ErrorKind.to_string exacto: bad_request, agent_error, infra_error.
  - SecretVal se inspecciona como ***REDACTED***.
  - default_sad_config() coincide con defaults canonicos.
- Casos incorrectos:
  - ErrorKind string distinto a SSOT.
  - Redaccion distinta de ***REDACTED***.
  - Default de SadConfig fuera de tabla canonica.

## Semantica de errores (obligatorio)
- ErrorKind esperado:
  - N/A (puro)
- ProblemDetails esperado:
  - N/A

## Evidence reproducible (obligatorio)
- Comandos:
  - gleam test
- Esperado:
  - Exit code 0
- Notas de auditoria:
  - Verificar default_sad_config() vs docs/plan/limits.md

## No-goals (obligatorio)
- Conversion Value <-> JSON (S04/S05).
- Loader TOML (S13).

## Dependencias
- S01-T01

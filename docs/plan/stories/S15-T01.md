# S15-T01 - Endpoint /artifacts/:artifact_id symlink-safe

## Sprint
- Sprint: S15
- Capa: Gateway
- Riesgo: Alto
- Gate: No

## SSOT (obligatorio)
- arquitectura/gateway.md - /artifacts/:artifact_id
- arquitectura/tipos.md - ArtifactRegistry invariantes
- arquitectura/tests.md - proxy/artifacts tests

## Inputs (obligatorio)
- OS/entorno:
  - Linux: Si
  - Docker CI: Si
  - Dependencias externas: None
- Config keys usadas (con defaults esperados):
  - limits.call_timeout_ms (default 30000)
- Fixtures exactos:
  - test/fixtures/source_local/runners/artifact_gen.py
- Supuestos:
  - lookup ArtifactRegistry fallido => 503

## Outputs (obligatorio)
- Codigo:
  - src/saar/gateway/artifacts_api.gleam:
    - GET /artifacts/:artifact_id (symlink-safe)
    - Content-Type desde mime
    - X-Content-Type-Options: nosniff
    - Content-Disposition: attachment
- Tests (inventario exacto):
  - test/integration/artifacts_test.gleam:
    - get_artifact_serves_file
    - get_artifact_auth_required
    - get_artifact_outside_workspace
    - artifact_after_agent_stopped
    - artifact_after_agent_deleted
- Make/targets:
  - Ninguno
- Artefactos runtime (si aplica):
  - workspace files

## Limites y defaults (obligatorio)
- limits.call_timeout_ms: 30000

## Acceptance Criteria (obligatorio)
- Casos correctos:
  - Sirve aunque el agente este Stopped.
  - Si falla symlink-safe o send_file => 404.
  - lookup Error(_) => 503.
- Casos incorrectos:
  - Devuelve 500 por path invalido o symlink.

## Semantica de errores (obligatorio)
- ErrorKind esperado:
  - infra_error solo si lookup falla (503)
- ProblemDetails esperado:
  - 404 en invalid path/symlink
  - 503 en lookup Error(_)

## Evidence reproducible (obligatorio)
- Comandos:
  - gleam test
- Esperado:
  - Exit code 0
- Notas de auditoria:
  - Verificar Content-Type y nosniff

## No-goals (obligatorio)
  - CORS (no se agrega)

## Dependencias
- S10-T04
